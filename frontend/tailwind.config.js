/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    darkMode: "class", // Prevents auto-dark mode based on system preference
    theme: {
        extend: {},
    },
    plugins: [],
}

